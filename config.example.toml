[publish]
artifact_directory = "artifacts"
state_directory = ".remarkable-state"

# Secrets are never configuration values. In Docker, credentials and SQLite state
# live in the remarkable-publish-state-v1 named volume.
